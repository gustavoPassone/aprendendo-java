package flamingo.aula.revisao;

public class OperadoresParte5 {
    // + simples
    // += soma e atribuição
    // -= subtração e atribuição
    // *= multiplicação e atribuição
    // /= divisão e atribuição
    // %= resto da divisão e atribuição

    static void main() {
        double bonus = 1000;
        bonus = bonus + 1000;

        bonus = bonus - 500;
        bonus += 1000;

        bonus /= 2;

        System.out.println();

    }

}
